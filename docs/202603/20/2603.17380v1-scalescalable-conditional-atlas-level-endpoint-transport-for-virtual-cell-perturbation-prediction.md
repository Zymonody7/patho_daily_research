---
title: "SCALE:Scalable Conditional Atlas-Level Endpoint transport for virtual cell perturbation prediction"
title_zh: SCALE：用于虚拟细胞扰动预测的可扩展条件图谱级终点传输
authors: "Shuizhou Chen, Lang Yu, Kedu Jin, Songming Zhang, Hao Wu, Wenxuan Huang, Sheng Xu, Quan Qian, Qin Chen, Lei Bai, Siqi Sun, Zhangyang Gao"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17380v1"
tags: ["query:bioinfo"]
score: 10.0
evidence: 细胞扰动预测的基础模型
tldr: 针对虚拟细胞扰动预测中训练效率低、高维稀疏建模不稳定以及评估指标偏离生物学意义的问题，本文提出了 SCALE 大模型。该模型基于 BioNeMo 框架优化了分布式扩展性，并采用基于 LLaMA 的细胞编码器与条件流匹配架构，将扰动预测建模为端点传输任务。在 Tahoe-100M 数据集上的实验表明，SCALE 在保持高效推理的同时，显著提升了生物学保真度指标，为大规模干预实验的计算模拟提供了更可靠的工具。
selection_source: fresh_fetch
motivation: 现有的虚拟细胞模型在处理大规模单细胞扰动预测时，面临计算效率低下、高维数据建模不稳定以及评估指标过于关注重建精度而非生物学真实性的挑战。
method: 构建了基于 BioNeMo 的高性能训练框架，并结合 LLaMA 架构与条件流匹配技术，通过端点监督实现稳定的细胞状态转换建模。
result: "在 Tahoe-100M 基准测试中，该模型相比现有 SOTA 方法在预训练速度上提升了 12.51 倍，且在 PDCorr 和差异表达基因重合度等关键生物学指标上分别提升了 12.02% 和 10.66%。"
conclusion: 推进虚拟细胞技术不仅需要更优的生成目标，还必须协同设计可扩展的基础设施、稳定的传输建模方法以及更符合生物学逻辑的评估体系。
---

## 摘要
虚拟细胞模型旨在通过单细胞测量数据预测细胞对遗传、化学或细胞因子扰动的反应，从而实现干法实验（in silico experimentation）。然而在实践中，大规模扰动预测仍受限于三个相互关联的瓶颈：低效的训练和推理流水线、高维稀疏表达空间中不稳定的建模，以及过度强调重建准确性而低估生物学保真度的评估协议。在这项工作中，我们提出了一个专门用于虚拟细胞扰动预测的大规模基础模型 SCALE，共同解决了上述局限性。首先，我们构建了一个基于 BioNeMo 的训练和推理框架，显著提高了数据吞吐量、分布式可扩展性和部署效率，在相同系统设置下，预训练速度比之前的 SOTA 流水线提升了 12.51 倍，推理速度提升了 1.29 倍。其次，我们将扰动预测表述为条件传输，并使用一种集合感知流架构来实现，该架构将基于 LLaMA 的细胞编码与面向终点的监督相结合。这种设计带来了更稳定的训练和更强的扰动效应恢复能力。第三，我们在 Tahoe-100M 上使用严格的细胞级协议对模型进行评估，该协议以具有生物学意义的指标为中心，而非仅仅关注重建。在该基准测试中，我们的模型相比 STATE 将 PDCorr 提高了 12.02%，DE Overlap 提高了 10.66%。总之，这些结果表明，推进虚拟细胞不仅需要更好的生成目标，还需要可扩展基础设施、稳定传输建模和生物学忠实评估的协同设计。

## Abstract
Virtual cell models aim to enable in silico experimentation by predicting how cells respond to genetic, chemical, or cytokine perturbations from single-cell measurements. In practice, however, large-scale perturbation prediction remains constrained by three coupled bottlenecks: inefficient training and inference pipelines, unstable modeling in high-dimensional sparse expression space, and evaluation protocols that overemphasize reconstruction-like accuracy while underestimating biological fidelity. In this work we present a specialized large-scale foundation model SCALE for virtual cell perturbation prediction that addresses the above limitations jointly. First, we build a BioNeMo-based training and inference framework that substantially improves data throughput, distributed scalability, and deployment efficiency, yielding 12.51* speedup on pretrain and 1.29* on inference over the prior SOTA pipeline under matched system settings. Second, we formulate perturbation prediction as conditional transport and implement it with a set-aware flow architecture that couples LLaMA-based cellular encoding with endpoint-oriented supervision. This design yields more stable training and stronger recovery of perturbation effects. Third, we evaluate the model on Tahoe-100M using a rigorous cell-level protocol centered on biologically meaningful metrics rather than reconstruction alone. On this benchmark, our model improves PDCorr by 12.02% and DE Overlap by 10.66% over STATE. Together, these results suggest that advancing virtual cells requires not only better generative objectives, but also the co-design of scalable infrastructure, stable transport modeling, and biologically faithful evaluation.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **SCALE**，一个旨在通过单细胞数据预测细胞在受到药物、基因敲除等“扰动”后如何反应的大规模基础模型。

### 1. 核心问题：虚拟细胞预测的“三大坑”
在生物信息学中，预测细胞对干预（如新药）的反应极具价值，但面临三个瓶颈：
*   **效率低：** 现有的模型训练和推理太慢，难以处理亿级（Atlas-level）的单细胞图谱数据。
*   **建模难：** 单细胞数据维度极高且极其稀疏，且由于测序是破坏性的，我们无法观察同一颗细胞“受药前”和“受药后”的连续变化，只能观察两群不同的细胞。
*   **指标偏：** 过去常看 MSE（均方误差），但 MSE 低并不代表生物学意义对。模型可能只是学会了预测“平均细胞”，而丢掉了关键的差异表达基因（DEGs，即受扰动后真正起反应的基因）。

### 2. 白话版概述
想象你要预测一群人吃药后的变化，但你手里只有“没吃药的一群人”和“吃了药的另一群人”的照片。SCALE 模型把这看作一个“运输问题”：它学习如何把“未处理细胞群”整体搬运到“处理后状态”。它借鉴了 LLaMA 的架构来理解基因间的关系，并利用 NVIDIA 的 BioNeMo 框架跑得飞快。最重要的是，它不再死磕数值上的精确，而是确保预测出的“哪些基因变活跃了”与真实情况高度一致。

### 3. 方法部分：端点传输与集合感知
*   **核心思想：** 将扰动预测建模为**条件端点传输（Endpoint Transport）**。既然中间过程不可见，模型就直接学习从起点（对照组）到终点（扰动组）的映射，而不是模拟复杂的中间路径。
*   **模型结构：**
    *   **分层编码器：** 内部是一个类似 LLaMA 的 Transformer，用于处理细胞内的基因依赖；外部套了一个 DeepSets 结构，确保模型处理细胞群体时具有“排列不变性”（即细胞的输入顺序不影响结果）。
    *   **条件流匹配（Flow Matching）：** 引入了 JiT（Just-in-Time）参数化技术。实验发现，直接预测终点（x-pred）比预测变化速度（v-pred）在生物学任务上更稳定。
*   **关键设计：** 放弃了传统的扩散模型路径，改用更简单的线性插值路径配合端点监督，减少了训练噪音。

### 4. 实验部分：亿级数据的硬核测试
*   **数据：** 使用了 **Tahoe-100M**（包含 1 亿个单细胞、1100 种药物处理）、PBMC（免疫信号）和 Replogle（基因筛选）三大数据集。
*   **Baseline：** 对比了 STATE、PerturbDiff、CPA 等当前最强的模型。
*   **评价指标：** 重点看 **PDCorr**（扰动相关性）和 **DE Overlap**（差异表达基因重合度），这些指标比 MSE 更能反映生物学真实性。
*   **主要结果：** 
    *   在 Tahoe-100M 上，PDCorr 提升了 **12.02%**，DE Overlap 提升了 **10.66%**。
    *   训练速度比之前的 SOTA（STATE）快了 **12.51 倍**。

### 5. 资源与算力
*   **框架：** 基于 NVIDIA 的 **BioNeMo** 框架开发，针对大规模分布式训练进行了优化。
*   **模型规模：** 提供了 184M（1.84 亿）和 280M（2.8 亿）参数两个版本。
*   **具体算力：** 文中未明确披露具体的 GPU 型号和数量，但强调了其在相同系统设置下通过优化数据加载（LMDB 分片）和并行策略实现了大幅加速。

### 6. 真正可信的贡献
*   **工程突破：** 证明了通过合理的存储优化（LMDB）和框架选择，虚拟细胞模型可以扩展到亿级数据。
*   **建模发现：** 证实了在单细胞扰动任务中，**“直接预测终点”优于“预测变化轨迹”**，这为后续生成式模型的设计指明了方向。
*   **指标纠偏：** 再次有力地证明了 MSE 越低可能生物学表现越差（“均值陷阱”），推动了社区转向更具生物学意义的评估体系。

### 7. 局限与风险
*   **规模饱和：** 实验显示模型增加到 280M 参数时，性能提升开始出现平台期（Saturate），单纯靠堆参数量可能不再有效。
*   **捷径学习（Shortcut）：** 如果对照组和扰动组差异很小，模型可能会学会简单的“复制粘贴”而不是真正的生物学逻辑。
*   **预处理敏感：** 评估结果高度依赖于数据预处理和归一化方式，不同实验室的代码实现可能导致结果波动。

### 8. 对 AI for Bioinformatics 的启发
*   **适合关注的人群：** 正在做单细胞基础模型、药物反应预测、以及关注大规模生物数据分布式训练的研究者。
*   **后续可跟进的问题：** 如何在模型中引入真实的生物学约束（如基因调控网络），以防止模型在处理从未见过的（Out-of-distribution）新药时产生幻觉？

（完）
