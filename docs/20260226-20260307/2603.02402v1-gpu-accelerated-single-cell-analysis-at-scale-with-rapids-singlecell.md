---
title: GPU-accelerated single-cell analysis at scale with rapids-singlecell
title_zh: 使用 rapids-singlecell 实现大规模 GPU 加速单细胞分析
authors: "Severin Dicks, Lukas Heumos, Lilly May, Sara Jimenez, Philipp Angerer, Ilan Gold, Isaac Virshup, Felix Fischer, Michelle Gill, Melanie Boerries, Corey J Nolet, Tiffany J. Chen, Fabian J. Theis"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.02402v1"
tags: ["query:seqai"]
score: 9.0
evidence: 用于大规模单细胞分析的GPU加速框架
tldr: 针对单细胞测序数据规模激增导致的 CPU 计算效率瓶颈，本文推出了 rapids-singlecell 框架。该框架基于 NVIDIA RAPIDS 和 CuPy，为 scverse 生态提供了原生的 GPU 加速支持，可直接处理 AnnData 结构。实验表明，它在预处理、聚类等核心环节实现了数百倍的加速，将千万级细胞的分析耗时从数小时缩短至分钟级，极大地提升了大规模单细胞研究的交互式探索能力。
selection_source: fresh_fetch
motivation: 传统的 CPU 分析流程在处理千万级规模的单细胞数据时耗时过长，严重阻碍了科研人员进行实时的交互式数据探索和假设验证。
method: 该框架利用 CuPy 数组和 NVIDIA RAPIDS 数据科学库，针对 AnnData 数据结构开发了一套可直接替换 scanpy 核心功能的 GPU 加速算子。
result: 在预处理、降维、图构建和聚类等标准工作流中，该方法相比优化后的 CPU 基准实现了高达数百倍的性能提升。
conclusion: rapids-singlecell 显著降低了超大规模单细胞分析的门槛，使研究者能够在标准硬件上实现近乎实时的参数调优和迭代分析。
---

## 摘要
单细胞测序技术以高分辨率揭示了细胞异质性，增进了我们对生物复杂性的理解。随着数据集规模扩展到数千万个细胞，计算工作流面临着巨大的瓶颈，基于 CPU 的分析流水线在过滤、归一化和聚类等常规处理步骤中需要数小时甚至数天的时间。这些可扩展性限制从根本上制约了常见的交互式数据探索和迭代假设检验。在此，我们介绍了 rapids-singlecell，这是一个 GPU 加速框架，它与 scverse 生态系统原生集成，并直接在 AnnData 数据结构上运行，为单细胞工作流提供了数量级的加速。rapids-singlecell 基于 CuPy 数组和 NVIDIA CUDA-X 数据科学 (RAPIDS) 生态系统构建，为基于 scanpy 的核心分析步骤提供了近乎即插即用的 GPU 替代方案。在预处理、降维、邻近图构建、聚类和批次校正等标准单细胞工作流中，与优化的 CPU 基准相比，rapids-singlecell 实现了高达数百倍的加速。这在标准硬件上将分析时间从数小时缩短至数分钟，同时保持了生物学解释的一致性。这些性能提升使得在无需拆分数据的情况下，近乎实时地分析大型数据集成为可能。结合实时参数调整和迭代工作流，rapids-singlecell 使交互式大规模单细胞分析成为现实。

## Abstract
Single-cell sequencing technologies reveal cellular heterogeneity at high resolution, advancing our understanding of biological complexity. As datasets start to scale to tens of millions of cells, computational workflows face substantial bottlenecks, with CPU-based analytical pipelines requiring hours or days for routine processing steps like filtering, normalization, and clustering. These scalability limitations fundamentally restrict common interactive data exploration and iterative hypothesis testing. Here we introduce rapids-singlecell, a GPU-accelerated framework that integrates natively with the scverse ecosystem and operates directly on the AnnData data structure, which delivers orders-of-magnitude speedups for single-cell workflows. Built on CuPy arrays and the NVIDIA CUDA-X Data Science (RAPIDS) ecosystem, rapids-singlecell provides near drop-in GPU replacements for core scanpy-based analysis steps. Across standard single-cell workflows such as preprocessing, dimensionality reduction, neighborhood graph construction, clustering, and batch correction, rapids-singlecell achieves speedups of up to several hundred-fold compared to optimized CPU baselines. This reduces analysis time from hours to minutes on standard hardware, while maintaining consistent biological interpretations. These performance improvements make it possible to analyze large data sets in close to real time, without the need for data splitting. Together with real-time parameter tuning and iterative workflows, rapids-singlecell makes interactive large-scale single-cell analysis possible.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **rapids-singlecell** 的 GPU 加速框架，旨在解决超大规模单细胞测序数据分析中的计算瓶颈问题。

### 1. 解决的问题与价值
*   **核心问题**：随着单细胞技术的发展，数据集规模已达千万级（如人类细胞图谱）。传统的 CPU 分析工具（如 Scanpy）在处理这些数据时，过滤、归一化、聚类等步骤动辄耗时数小时甚至数天。
*   **研究价值**：计算缓慢严重阻碍了“交互式探索”。科研人员无法实时调整参数并观察结果，这限制了科学假设的迭代速度。该工具将分析时间从“小时级”缩短到“秒级”，让千万级细胞的实时分析成为可能。

### 2. 白话版概述
单细胞分析就像是在一个拥有几千万个零件的仓库里进行分类和绘图。目前的工具（基于 CPU）像是一个人在手工搬运和记录，速度极慢。这篇论文提供了一套“全自动高速机器人流水线”（基于 GPU），它不仅搬运速度快了几百倍，而且完全兼容现有的“仓库管理标准”（AnnData 数据结构）。研究者不需要重写代码，只需换几个函数名，就能在几十秒内完成以前要跑一小时的任务。

### 3. 方法部分：核心设计与实现
*   **核心思想**：利用 GPU 的大规模并行计算能力，将数据保留在显存中，减少 CPU 与 GPU 之间的数据传输开销，并针对单细胞数据的**稀疏性**（大部分基因表达值为 0）进行深度优化。
*   **技术栈**：
    *   **CuPy**：作为 NumPy/SciPy 的 GPU 替代品，处理稀疏矩阵运算。
    *   **RAPIDS (cuML/cuGraph)**：提供机器学习（PCA、UMAP）和图算法（Leiden 聚类）的底层加速。
    *   **自定义 CUDA 内核**：针对生物信息学特有的步骤（如高变基因选择、归一化）编写了专门的 C++/CUDA 代码，以实现极致性能。
*   **关键设计取舍**：
    *   **内存管理**：引入 RMM（RAPIDS Memory Manager），支持“内存溢出”处理。当显存不足时，自动利用系统内存，保证程序不崩溃，虽然速度会变慢，但提升了通用性。
    *   **分布式扩展**：集成 Dask 框架，支持多 GPU 并行和“分块计算”，从而突破单张显卡显存的限制，处理亿级细胞。

### 4. 实验部分
*   **数据与任务**：使用了 130 万个小鼠脑细胞、1140 万个胚胎细胞以及 1 亿个细胞的超大型数据集。任务涵盖了从预处理到批次校正（Harmony）的全流程。
*   **Baseline（基准）**：高性能 32 核 CPU 工作站（AMD Threadripper Pro）。
*   **主要结果**：
    *   **速度提升**：在 130 万细胞的标准流程中，GPU 仅需 **26 秒**，而 CPU 需要 **52 分钟**（加速约 120 倍）。
    *   **特定算法**：UMAP 降维加速达 350 倍；Harmony 批次校正（用于整合不同来源的数据）在 1100 万细胞上仅需 25 秒，而 CPU 版本在 2 小时内无法完成。
    *   **一致性**：实验证明，尽管 GPU 存在浮点数运算顺序差异，但最终的生物学结论（细胞分群、标记基因识别）与 CPU 结果高度一致。

### 5. 资源与算力
*   **硬件支持**：测试涵盖了 NVIDIA DGX B200、H200 等数据中心级显卡，以及 RTX PRO 6000 (Blackwell) 和 RTX 5090 等消费级/工作站显卡。
*   **软件环境**：基于 Python，集成在 scverse 生态中。

### 6. 真正可信的贡献
*   **原生集成 AnnData**：这是目前单细胞领域最通用的数据格式，rapids-singlecell 实现了“无缝替换”，用户迁移成本极低。
*   **重构了 Harmony 算法**：通过重新设计 GPU 上的线性代数求解器，彻底解决了大规模样本整合的计算难题。
*   **验证了亿级规模的可行性**：在 8 张 B300 GPU 上，20 分钟内跑完 1 亿个细胞的全流程，树立了行业新标杆。

### 7. 局限与风险
*   **小数据集无优势**：对于小于 5 万个细胞的数据集，由于数据传输和内核启动的开销，GPU 可能并不比 CPU 快。
*   **硬件依赖**：目前仅支持 NVIDIA CUDA 架构，AMD 或 Apple 芯片用户无法直接使用。
*   **随机性差异**：由于 GPU 并行计算的特性，聚类结果在位级别（bit-level）上无法与 CPU 完全对齐，虽然生物学意义相同，但在严苛的审计环境下可能需要额外说明。

### 8. 对 AI for Bioinformatics 的启发
*   **适合关注的人群**：从事大规模单细胞图谱构建、空间转录组分析、以及需要频繁进行参数调优的生物信息学研究者。
*   **后续可跟进的问题**：如何将该加速框架与当前的单细胞大模型（如 scGPT, Geneformer）结合，实现从预处理到模型推理的全链路 GPU 加速？

（完）
