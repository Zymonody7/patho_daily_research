---
title: "HarmonyCell: Automating Single-Cell Perturbation Modeling under Semantic and Distribution Shifts"
title_zh: HarmonyCell：语义与分布偏移下的自动化单细胞扰动建模
authors: "Wenxuan Huang, Mingyu Tsoi, Yanhao Huang, Xinjie Mao, Xue Xia, Hao Wu, Jiaqi Wei, Yuejin Yang, Lang Yu, Cheng Tan, Xiang Zhang, Zhangyang Gao, Siqi Sun"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01396v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于单细胞扰动建模的LLM驱动Agent
tldr: "针对单细胞扰动建模中元数据格式不一（语义异构）和生物变异导致的分布偏移（统计异构）难题，HarmonyCell 提出了一个端到端的智能体框架。该框架利用大模型驱动的语义统一器自动对齐异构元数据，并结合蒙特卡洛树搜索（MCTS）在层级空间内自动合成具有最优归纳偏置的模型架构。实验表明，该方法在异构数据集上的执行成功率达95%，且在分布外预测任务中达到或超越了专家设计的基准模型，实现了无需人工干预的自动化虚拟细胞建模。"
selection_source: fresh_fetch
motivation: 现有的单细胞扰动研究受限于不同数据集间元数据标准不一以及生物学差异带来的分布偏移，导致模型难以跨数据集通用且需要大量人工调优。
method: 采用大模型驱动的语义统一器自动映射异构元数据，并利用蒙特卡洛树搜索在层级动作空间中搜索最适合特定分布特征的模型架构。
result: "在处理异构输入时实现了95%的有效执行率，并在严苛的分布外（OOD）评估中表现出优于或等同于专家手工设计模型的预测性能。"
conclusion: HarmonyCell 通过自动化语义对齐和架构搜索，解决了单细胞建模中的异构性瓶颈，为大规模、自动化的虚拟细胞实验提供了可扩展的解决方案。
---

## 摘要
单细胞扰动研究面临双重异质性瓶颈：(i) 语义异质性——相同的生物学概念在不同数据集间以不兼容的元数据模式进行编码；(ii) 统计异质性——生物变异引起的分布偏移要求特定于数据集的归纳偏置。我们提出了 HarmonyCell，这是一个端到端的智能体框架，通过专用机制解决这些挑战：LLM 驱动的语义统一器（Semantic Unifier）无需人工干预即可自主将异构元数据映射到规范接口；自适应蒙特卡洛树搜索（MCTS）引擎在分层动作空间上运行，以合成针对分布偏移具有最优统计归纳偏置的架构。在语义和分布偏移下的多种扰动任务评估中，HarmonyCell 在异质输入数据集上实现了 95% 的有效执行率（相比之下，通用智能体为 0%），同时在严格的分布外评估中达到甚至超过了专家设计的基准模型。这种双轨编排实现了可扩展的自动化虚拟细胞建模，无需针对特定数据集进行工程设计。

## Abstract
Single-cell perturbation studies face dual heterogeneity bottlenecks: (i) semantic heterogeneity--identical biological concepts encoded under incompatible metadata schemas across datasets; and (ii) statistical heterogeneity--distribution shifts from biological variation demanding dataset-specific inductive biases. We propose HarmonyCell, an end-to-end agent framework resolving each challenge through a dedicated mechanism: an LLM-driven Semantic Unifier autonomously maps disparate metadata into a canonical interface without manual intervention; and an adaptive Monte Carlo Tree Search engine operates over a hierarchical action space to synthesize architectures with optimal statistical inductive biases for distribution shifts. Evaluated across diverse perturbation tasks under both semantic and distribution shifts, HarmonyCell achieves a 95% valid execution rate on heterogeneous input datasets (versus 0% for general agents) while matching or even exceeding expert-designed baselines in rigorous out-of-distribution evaluations. This dual-track orchestration enables scalable automatic virtual cell modeling without dataset-specific engineering.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **HarmonyCell** 的端到端 AI 智能体（Agent）框架，旨在自动化单细胞扰动建模（即预测细胞在受到药物或基因编辑等干预后的反应）。

### 1. 解决的问题与价值
在生物信息学中，构建“虚拟细胞”模型面临两个核心痛点：
*   **语义异质性（数据乱）**：不同实验室的数据集格式五花八门。比如，有的把药物名称列叫 `drug_name`，有的叫 `compound`；有的基因用 ID，有的用符号。目前这需要大量人工手动清洗。
*   **统计异质性（模型难调）**：生物实验存在批次效应和分布偏移（OOD）。针对 A 数据集表现好的模型架构（如 VAE），在 B 数据集上可能不如简单的回归模型。研究者需要反复试错来寻找最优架构。

**价值**：它实现了从“原始杂乱数据”到“高性能预测模型”的全自动转化，执行成功率极高，且预测精度达到甚至超过了人类专家设计的模型。

### 2. 白话版概述
想象你手里有几十个不同实验室传来的 Excel 表格，每个表格的表头都不一样，你得预测细胞吃药后的变化。
1.  **HarmonyCell** 先派出一个“翻译官”（LLM），把这些乱七八糟的表头自动对齐成标准格式。
2.  然后它派出一个“架构师”（基于蒙特卡洛树搜索的引擎），像下围棋一样在成千上万种模型组合中寻找最合适的那一个。
3.  它不仅会写代码，还会根据运行结果自动 Debug，最后交给你一个针对这组数据优化过的预测模型。

### 3. 方法部分：核心设计
*   **语义统一器（Semantic Unifier）**：利用大语言模型（LLM）的推理能力，读取原始数据的元数据描述，生成一个 JSON 映射表。它不仅能改名，还能处理逻辑（如：将微摩尔单位统一转为纳摩尔）。
*   **分层动作空间的 MCTS 引擎**：
    *   **核心思想**：将模型设计拆解为三个层级：**策略层**（选生成式还是判别式）、**架构层**（选 ResNet、Transformer 还是 VAE）、**工程层**（调超参数和损失函数）。
    *   **搜索流程**：采用蒙特卡洛树搜索（MCTS）。它会根据历史经验（知识库）进行“热启动”，然后通过“选择-扩展-模拟-回溯”的循环，不断试错并优化模型。
    *   **关键取舍**：放弃了让 LLM 直接写全部代码的方案（容易出错），转而让 LLM 在预定义的“生物模型组件库”中做选择题，极大地提高了代码的运行成功率。

### 4. 实验部分
*   **数据与任务**：使用了 Srivatsan（药物扰动）、Adamson/Norman/Replogle（基因编辑）等权威数据集。任务包括预测未见过的药物反应和未见过的细胞类型反应。
*   **Baseline（基准）**：对比了 CPA、Biolord、Sams VAE 等由人类专家设计的顶级模型，以及 AIDE 等通用编程 Agent。
*   **评价指标**：RMSE（误差）、DeltaPCC（变化趋势的相关性）、CosLogFC（变化方向的相似度）。
*   **主要结果**：
    *   **鲁棒性**：在处理杂乱元数据时，HarmonyCell 的执行成功率为 **95%**，而通用 Agent 为 0%。
    *   **性能**：在 Norman 数据集上，其 DeltaPCC 达到 0.62，远超最强基准模型（0.44）。
    *   **协同效应**：它能自动合并两个不同来源的数据集进行联合训练，并产生正向迁移效果。

### 5. 资源与算力
*   **LLM 后端**：使用了 Google Gemini 1.5 Flash (thinking 版本)。
*   **搜索开销**：每个任务的搜索时间限制在 **5 小时** 内，通常进行 16-32 次节点扩展。
*   **算力硬件**：文中提到了使用 CUDA 加速，但未具体披露显卡型号和数量（通常此类任务需要单张 A100 或同级别显卡）。

### 6. 真正可信的贡献
*   **端到端自动化**：最扎实的贡献是证明了 LLM 可以处理生物信息学中极其琐碎的“数据清洗”工作，且成功率极高。
*   **分层搜索的有效性**：消融实验清晰地证明了，相比于扁平的搜索，分层架构搜索能更快地收敛到高性能模型。
*   **OOD 适应力**：证据显示，该 Agent 发现的“判别式+残差网络”架构在特定基因扰动任务上确实优于传统的生成式模型。

### 7. 局限与风险
*   **搜索成本**：每个新数据集都要跑 5 小时搜索，相比于直接使用预训练好的大模型（Foundation Model），实时性较差。
*   **组件库限制**：Agent 的创造力受限于预定义的代码组件库。如果最优解需要一种全新的数学结构，该 Agent 无法凭空创造。
*   **模态单一**：目前仅支持转录组（RNA-seq）数据，尚未扩展到蛋白质组或空间转录组。

### 8. 对 AI for Bioinformatics 的启发
*   **适合关注的人群**：正在开发生物学 Agent、自动化实验室（Self-driving Lab）或从事单细胞多组学分析的研究者。
*   **后续可跟进的问题**：能否将这种“搜索架构”的思路引入到多模态数据对齐中？或者让 Agent 自主发现新的生物学损失函数（Loss Function）而不仅仅是挑选现有的？

（完）
