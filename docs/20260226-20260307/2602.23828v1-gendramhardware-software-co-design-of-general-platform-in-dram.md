---
title: "GenDRAM:Hardware-Software Co-Design of General Platform in DRAM"
title_zh: GenDRAM：DRAM内通用平台的软硬件协同设计
authors: "Tsung-Han Lu, Weihong Xu, Tajana Rosing"
date: 2026-02-27
pdf: "https://arxiv.org/pdf/2602.23828v1"
tags: ["query:seqai"]
score: 9.0
evidence: 用于基因组序列比对和工作流的PIM加速器
tldr: 针对动态规划算法（如全源最短路径和基因比对）在传统架构中面临的严重数据传输瓶颈，以及现有存内计算方案无法处理完整工作流的问题，本文提出了GenDRAM。该架构利用单片3D DRAM（M3D DRAM）的高带宽和容量，集成了专门的搜索处理单元和无乘法器通用计算单元，并配合3D感知数据映射策略。实验显示，GenDRAM在APSP任务上比顶级GPU快68倍，在基因组端到端流程中快22倍，显著提升了数据密集型任务的执行效率。
selection_source: fresh_fetch
motivation: 传统计算架构在处理动态规划等数据密集型算法时，受限于内存与处理器间的数据搬运效率，且现有存内计算方案难以覆盖完整的业务流程。
method: 提出一种基于单片3D DRAM的软硬协同设计方案，通过集成搜索专用单元与通用计算单元，并结合3D感知的延迟优化映射策略，实现全流程存内加速。
result: 实验结果表明，GenDRAM在全源最短路径算法上实现了68倍于GPU的性能提升，并在基因组测序全流程中取得了22倍的加速效果。
conclusion: 该研究证明了通过在3D DRAM中深度集成异构计算单元，可以有效消除系统级通信瓶颈，为大规模数据密集型应用提供高性能通用平台。
---

## 摘要
动态规划（DP）算法，如全源最短路径（APSP）和基因组序列比对，是许多科学领域的基础，但在传统架构上受到数据移动的严重制约。虽然存内计算（PIM）提供了一个有前景的解决方案，但现有的加速器通常只处理工作流的一小部分，从而在主机-加速器通信和片外数据流传输中产生了新的系统级瓶颈。在这项工作中，我们提出了 GenDRAM，一种克服了这些局限性的大规模并行 PIM 加速器。GenDRAM 利用单片三维 DRAM（M3D DRAM）的巨大容量和内部带宽，将整个数据密集型流水线（例如从种子生成到比对的完整基因组工作流）集成到单个异构芯片上。其核心是一种新颖的架构，具有用于内存密集型任务的专用搜索处理单元（Search PUs）和用于各种 DP 计算的通用、无乘法器计算处理单元（Compute PUs）。这是通过一种 3D 感知的数据映射策略实现的，该策略利用 M3D DRAM 的分层延迟进行性能优化。通过全面的仿真，我们证明了 GenDRAM 实现了变革性的性能飞跃，在 APSP 上优于最先进的 GPU 系统 68 倍以上，在端到端基因组流水线上优于 22 倍以上。

## Abstract
Dynamic programming (DP) algorithms, such as All-Pairs Shortest Path (APSP) and genomic sequence alignment, are fundamental to many scientific domains but are severely bottlenecked by data movement on conventional architectures. While Processing-in-Memory (PIM) offers a promising solution, existing accelerators often address only a fraction of the work-flow, creating new system-level bottlenecks in host-accelerator communication and off-chip data streaming. In this work, we propose GenDRAM, a massively parallel PIM accelerator that overcomes these limitations. GenDRAM leverages the immense capacity and internal bandwidth of monolithic 3D DRAM(M3D DRAM) to integrate entire data-intensive pipelines, such as the full genomics workflow from seeding to alignment, onto a single heterogeneous chip. At its core is a novel architecture featuring specialized Search PUs for memory-intensive tasks and universal, multiplier-less Compute PUs for diverse DP calculations. This is enabled by a 3D-aware data mapping strategy that exploits the tiered latency of M3D DRAM for performance optimization. Through comprehensive simulation, we demonstrate that GenDRAM achieves a transformative performance leap, outperforming state-of-the-art GPU systems by over 68x on APSP and over 22x on the end-to-end genomics pipeline.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **GenDRAM** 的软硬件协同设计方案，旨在利用先进的 3D 堆叠内存技术解决生物信息学和图计算中的“内存墙”问题。

### 1. 核心问题与研究价值
动态规划（DP）算法是基因组测序（如序列比对）和图分析（如最短路径 APSP）的核心。这类算法的共同痛点是：**计算简单但数据量极大**。
*   **现有瓶颈：** 即使使用 GPU 加速，大量时间也浪费在内存与处理器之间搬运数据上。
*   **PIM 的局限：** 之前的“存内计算”（PIM）方案通常只加速比对（Alignment）这一步，而忽略了同样耗时的种子生成（Seeding，即在参考基因组中寻找潜在匹配位置的过程）。这导致数据仍需在主机和加速器之间频繁交换，产生了新的通信瓶颈。

### 2. 白话版概述
想象你要在一万本书里找一段话。传统的做法是把书一本本搬到书桌（CPU/GPU）上看，搬书的时间远超阅读时间。GenDRAM 的做法是直接在书架（3D 内存）里装上很多微型摄像头和大脑。它不仅能快速翻书找关键词（种子生成），还能原地进行精细的文字对比（序列比对）。通过把整个分析流程都关在“书架”里完成，它彻底消除了搬书的麻烦，速度比顶级显卡快了几十倍。

### 3. 方法部分：核心设计与取舍
GenDRAM 基于 **单片 3D DRAM (M3D DRAM)**，这是一种比 HBM 带宽更高、延迟更低的堆叠内存技术。

*   **异构处理单元 (PU) 设计：**
    *   **搜索单元 (Search PUs)：** 专门处理内存密集型的“种子生成”任务，优化了不规则的查表操作。
    *   **计算单元 (Compute PUs)：** 采用无乘法器设计，专为加法和比较操作（动态规划的核心）优化，节省面积和功耗。
*   **3D 感知数据映射 (3D-Aware Mapping)：**
    *   **物理特性利用：** 3D 内存层数越多，顶层延迟越高。GenDRAM 将最常访问的“热数据”（如索引表）放在靠近逻辑层的底层（低延迟区），将大容量参考序列放在高层。
*   **可重构执行流：**
    *   **模式一（同构）：** 处理图计算（APSP）时，所有单元协同广播数据。
    *   **模式二（异构流水线）：** 处理基因组任务时，搜索单元作为“生产者”找位置，计算单元作为“消费者”做比对，实现并行流水线。
*   **关键取舍：** 放弃了通用计算能力，换取了极高的单位面积计算密度和极低的数据搬运开销。

### 4. 实验部分
*   **数据与任务：**
    *   **图计算：** 使用 SNAP 和 OpenStreetMap 真实图数据集测试全源最短路径（APSP）。
    *   **基因组：** 使用人类参考基因组 GRCh38，模拟 Illumina（短读段）和 PacBio/ONT（长读段）数据。
*   **Baseline（对比对象）：** NVIDIA A100/H100 GPU、Intel Xeon CPU、以及之前的 PIM 加速器（如基于 ReRAM 的 RapidGraph）。
*   **主要结果：**
    *   **性能：** 在 APSP 任务上比 A100 快 **68 倍**；在端到端基因组流程上比 A100 快 **22 倍**。
    *   **能效：** 基因组比对的能效比 GPU 高出 **152 倍**。
    *   **功耗：** 运行功率约 31.2W，符合 3D 堆叠芯片的散热要求。

### 5. 资源与算力
*   **训练/开发资源：** 文中未涉及模型训练（属于硬件架构仿真）。
*   **仿真工具：** 使用 Verilog 进行逻辑设计，Synopsys Design Compiler 进行 7nm 工艺综合，使用自定义的周期精确（Cycle-accurate）仿真器评估性能。

### 6. 真正可信的贡献
*   **全流程集成：** 证明了在内存中集成“搜索+计算”异构单元可以消除系统级通信瓶颈，这是比单纯加速某个算子更有意义的系统级优化。
*   **物理特性结合：** 巧妙利用了 3D DRAM 垂直方向上的延迟差异（Tiered Latency）来优化数据布局，这一结论证据充分且符合半导体物理特性。
*   **数学抽象：** 将图算法和序列比对统一抽象为“广义网格更新问题”，为通用 PIM 平台提供了理论基础。

### 7. 局限与风险
*   **制造挑战：** 单片 3D DRAM（M3D）技术目前仍处于前沿实验室阶段，量产成本和良率远不如成熟的 HBM。
*   **散热风险：** 虽然仿真显示功耗在 30W 左右，但在 1024 层超薄 DRAM 下方放置高性能逻辑层，真实的局部热点（Hotspot）管理可能比模拟的更复杂。
*   **灵活性不足：** 该架构高度针对动态规划类算法，如果生物信息学算法转向基于深度学习（如 Transformer）的方案，该硬件的利用率将大幅下降。

### 8. 对 AI for Bioinformatics 的启发
*   **适合关注的人群：** 从事大规模基因组比对、实时图数据库加速、以及底层硬件架构设计的开发者。
*   **后续可跟进的问题：** 如何将这种存内计算架构扩展到支持大语言模型（LLM）中的长序列注意力机制（Attention）计算？

（完）
